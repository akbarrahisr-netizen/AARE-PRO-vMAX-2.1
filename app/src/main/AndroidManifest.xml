<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ==================== NETWORK PERMISSIONS ==================== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ==================== SMS PERMISSIONS (OTP Auto-fill) ==================== -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <!-- ==================== FOREGROUND SERVICE PERMISSIONS (Captcha Screenshot) ==================== -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <!-- ==================== SYSTEM & NOTIFICATION PERMISSIONS ==================== -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- ==================== ACCESSIBILITY SERVICE ==================== -->
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />

    <!-- ==================== QUERIES (UPI Apps for Payment) ==================== -->
    <queries>
        <package android:name="cris.org.in.prs.ima" />
        <package android:name="in.org.npci.upiapp" />
        <package android:name="net.one97.paytm" />
        <package android:name="com.google.android.apps.nbu.paisa.user" />
        <package android:name="com.phonepe.app" />
        <package android:name="com.hdfcbank.payzapp" />
        <package android:name="com.msf.kbank.mobile" />
        <package android:name="com.amazon.mShop.android.shopping" />
    </queries>

    <application
        android:allowBackup="true"
        android:label="VMAX Pro Sniper"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
        android:usesCleartextTraffic="true"
        android:hardwareAccelerated="true">

        <!-- ✅ ML Kit Dependencies (Fast OCR) -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="ocr,tflite" />

        <!-- ✅ Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ Accessibility Service (Workflow Engine) -->
        <service
            android:name=".core.engine.WorkflowEngine"
            android:exported="true"
            android:label="VMAX Sniper Engine"
            android:foregroundServiceType="specialUse|mediaProjection"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
            
            <property
                android:name="android.foreground_service_type_special_use"
                android:value="Automation for accessibility and railway booking assistance for optimized speed." />
        </service>

    </application>

</manifest>
